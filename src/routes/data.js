import Data from "../pages/Data";

const root = "/data";

const data = [
    {
        path: `${root}`,
        component: <Data/>,
        navbar: true,
        sidebar: true,
    },
];

export default data;
